apiVersion: v1
kind: Service
metadata:
  name: {{ include "silly-helm-repo.fullname" . }}
  labels:
    {{- include "silly-helm-repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "silly-helm-repo.selectorLabels" . | nindent 4 }}
